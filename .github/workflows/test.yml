name: Test Create Issue on Failure

on:
  push:
    branches:
      - main

permissions:
  issues: write
  actions: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Step A
        if: always()
        run: exit 1

      - name: Step B
        if: always()
        run: echo "Step B passed"

      - name: Step C
        if: always()
        run: exit 1

      - if: failure()
        uses: DmitriyLewen/create-issue-on-failure@5962d430d4bc47f2c64ae6ddbc8c9b8a76534a00
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
